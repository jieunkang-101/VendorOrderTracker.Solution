@using VendorOrderTracker.Models;
@{
    Layout = "_Layout";
}

<div class="container">
  <br>
  <h1>Vendors</h1>
  <br>
  <hr>
  <br>
  @if (@Model.Count == 0)
  {
    <h4>No Vendors have been added yet!</h4>
  }

  @foreach (Vendor vendor in Model)
  {
    <div class="card">
      <div class="card-header">
        <h4>@vendor.Name</h4>
      </div>  
      <div class="card-body">
        <h5 class="card-title">Vendor Description: </h5>
        <p class="card-text">@vendor.Description</p>
      </div>
      <div class="card-footer"> 
        <div class="row">
          <div class="col-md-4">
            <form action='/vendors/@vendor.Id'>
              <button class="btn" type="submit">View/Add Order</button>
            </form>
          </div>
          <div class="col-md-4">
            <form action='/vendors/@vendor.Id/edit'>
              <button class="btn" type="submit">Edit Vendor Info</button>
            </form>
          </div>
          <div class="col-md-4">
            <form action='/vendors/@vendor.Id/delete'>
              <button class="btn" type="submit">Delete this Vendor</button>
            </form>
          </div>
        </div>    
      </div>   
    </div>  
  }
</div>